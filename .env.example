# MongoDB Atlas Connection String
# Get this from MongoDB Atlas -> Connect -> Connect your application
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.your-subdomain.mongodb.net/supplier_scorecard?retryWrites=true&w=majority

# Session secret key
SESSION_SECRET=zirium-vendor-scorecard-secret-2026

# Port (for local run)
PORT=3000
